
  # Create Widgets

  This is a code bundle for Create Widgets. The original project is available at https://www.figma.com/design/jd5cHbMWPWRNXXfp52YbVU/Create-Widgets.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  