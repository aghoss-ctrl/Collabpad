{
  "name": "blind-padlet-widget",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run bundle -- --minify",
    "watch": "npm run bundle -- --watch",
    "bundle": "esbuild widget-src/code.tsx --bundle --outfile=dist/code.js --target=es6"
  },
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "@figma/widget-typings": "*",
    "esbuild": "*",
    "typescript": "*"
  }
}
